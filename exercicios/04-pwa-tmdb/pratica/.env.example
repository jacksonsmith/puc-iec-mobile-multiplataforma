# Copie este arquivo para .env.local (sem .example) e preencha.
# .env.local está no .gitignore — NUNCA comite tokens reais.
#
# Gere em: https://www.themoviedb.org/settings/api
# Use o API Read Access Token (v4, JWT longo).

VITE_TMDB_TOKEN=cole_seu_token_aqui
