# Atividade 3 — Aluno: jacksonsmith (teste)

Native module RN ↔ Kotlin + Swift via TurboModule. Inclui relatorio.md comparativo.

## Como rodar

```bash
yarn codegen
cd ios && pod install && cd ..
npx expo run:ios
npx expo run:android
```
